#include <stdio.h>

int main() {

	int temperatura;

	printf("Qual a temperatura? ");
	scanf("%f", &temperatura);

	if (temperatura >= 40) {
		printf("Como você está vivo?.\n");
	}

	else if (temperatura >= 30) {
		printf("Clima quente, otimo para ir à praia.\n");
	}

	else if (temperatura >= 20 ) {
		printf("Clima agradavel.\n");
	}

	else if (temperatura >= 10) {
		printf("Clima Frio. Não esqueça o casaco")
	}

	else if

}